# m6APrediction
The m6APrediction package provides tools to encode RNA sequences and      predict potential m6A modification sites using a trained random forest model.      It includes helper functions for DNA sequence encoding and for predicting      m6A status on single or multiple RNA entries.
---
title: "m6APrediction: Predicting m6A Sites"
output: html_document
---

# m6APrediction

`m6APrediction` 提供两个导出函数用于基于序列特征预测 m6A 位点，并包含绘制 ROC/PRC 的工具。

## 安装

```r
# 从 GitHub 安装（替换为你的 GitHub 用户名）
remotes::install_github("XinruSong23/m6APrediction")
